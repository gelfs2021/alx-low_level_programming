#include <stdio.h>

/**
 * main- this function prints "the phrase with proper grammar,
 * but the outcome is a piece of art,"
 * Return: if exited properly,
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
