#include <stdio.h>

/**
 * main - prints all possible combinations
 * Return: it will return 0 if it works correctly
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
		if (n != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
