#include <stdio.h>

/**
 * main - prints all single digigit
 * 10 starting from 0, followed by a new line
 * Return: it will return 0 if it works correctly
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
